---
import GettingAroundBody from "../../../components/pages/GettingAroundBody.astro";
import { prefixedLocales } from "../../../i18n/config";

export function getStaticPaths() {
  return prefixedLocales.map((lang) => ({ params: { lang } }));
}
---

<GettingAroundBody />
